{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "installCommand": "npm install",
        "buildCommand": "npm run build"
      }
    }
  ],
  "env": {
    "SHOPIFY_CLI_PATH": "./node_modules/.bin/shopify"
  }
}